name: Apify to Webflow Automation

on:
  schedule:
    - cron: "0 */24 * * *"  # every 24 hours
  workflow_dispatch:       # allows manual run button

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Run Apify → Webflow script
        run: |
          echo "This is where your automation will run"
